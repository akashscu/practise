# use task view to see the specific set of package for your requirements
# install.packages("slidify")
# if we  need to install more than one packages at a time then 
# install.packages(c("slidify","ggplot2"))
# if you wanna install package from biocondutor
## source("http://biocondutor.org/bioclite.R")
#bioclite()
# but for specific package can be done similarly 
# bioclite(c("package1","package2"))
# http://www.bioconductor.org/install
# if you have install any package and want to use that package then you need to install 
# that package by specifying
## library(ggplot2)